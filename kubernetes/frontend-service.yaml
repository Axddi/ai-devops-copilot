apiVersion: v1
kind: Service

metadata:
  name: ai-devops-frontend
  namespace: ai-devops

spec:

  selector:
    app: ai-devops-frontend

  ports:

    - port: 3000

      targetPort: 3000

  type: LoadBalancer